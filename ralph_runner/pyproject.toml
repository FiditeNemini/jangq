[project]
name = "jang-ralph-runner"
version = "0.1.0"
description = "Autonomous test harness for JANG Studio"
requires-python = ">=3.11"
dependencies = [
    "ruamel.yaml>=0.18",
    "huggingface_hub>=0.20",
    "rich>=13.0",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]
